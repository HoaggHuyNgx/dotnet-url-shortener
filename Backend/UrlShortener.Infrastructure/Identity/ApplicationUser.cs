using Microsoft.AspNetCore.Identity;

namespace UrlShortener.Infrastructure.Identity;

public class ApplicationUser : IdentityUser
{
    // Bạn có thể thêm các thuộc tính tùy chỉnh ở đây trong tương lai
}
